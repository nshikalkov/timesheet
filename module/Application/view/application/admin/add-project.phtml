<h1>Add a customer</h1>

<?php
$form = $this->form;
$form->setAttribute('action', $this->url('admin', ['action' => 'add-project', 'id' => $this->customerId]));
echo $this->partial('application/admin/projectform', [
    'form' => $form,
    'submitLabel' => 'Insert new project',
]);
